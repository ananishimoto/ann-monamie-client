export const selectProjects = (state) => state.project.byStatus;

export const selectDetails = (state) => state.project.details;

export const inspirationProjects = (state) => state.project.suggestions;
